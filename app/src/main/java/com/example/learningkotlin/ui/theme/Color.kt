package com.example.learningkotlin.ui.theme

import androidx.compose.ui.graphics.Color

// HEVY STYLE PALETTE

// 1. The Blues (Primary Action Color)
val HevyBlue = Color(0xFF2196F3)        // Bright Blue for Buttons/Text
val HevyBlueDark = Color(0xFF1976D2)    // Slightly darker for gradients/pressed states

// 2. The Darks (Backgrounds)
val HevyBlack = Color(0xFF000000)       // Pure Black background
val HevyDarkGrey = Color(0xFF1C1C1E)    // Card Background (The "Hevy" Card Color)
val HevyInputGrey = Color(0xFF2C2C2E)   // Input Field Background (Slightly lighter)

// 3. The Lights (Text)
val HevyWhite = Color(0xFFFFFFFF)       // Primary Text
val HevyLightGrey = Color(0xFF8E8E93)   // Secondary Text (Labels like "KG")

// 4. Status Colors
val HevyGreen = Color(0xFF34C759)       // Success / Done
val HevyRed = Color(0xFFFF3B30)         // Delete / Error / Failure Set
val HevyOrange = Color(0xFFFFA500)      // Warmup Set
val HevyPurple = Color(0xFFAF52DE)      // Drop Set